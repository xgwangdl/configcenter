server:
  port: 9001
eureka:
  instance:
    appname: gateway
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
      
zuul:
  routes:
    client-a:
      patch: /userinfo/**
      serviceId: client-a
